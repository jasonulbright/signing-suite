<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Signing Suite" Width="1180" Height="780" MinWidth="860" MinHeight="560"
        WindowStartupLocation="CenterScreen" AllowDrop="True"
        FontFamily="Segoe UI" FontSize="13" Background="#FFF5F6F8">
  <Window.Resources>
    <Style TargetType="Button">
      <Setter Property="Padding" Value="12,4"/>
      <Setter Property="MinWidth" Value="84"/>
    </Style>
    <Style x:Key="Card" TargetType="Border">
      <Setter Property="Background" Value="White"/>
      <Setter Property="BorderBrush" Value="#FFD5D9E0"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="CornerRadius" Value="4"/>
      <Setter Property="Padding" Value="12,8"/>
    </Style>
    <Style x:Key="Caption" TargetType="TextBlock">
      <Setter Property="FontSize" Value="11"/>
      <Setter Property="Foreground" Value="#FF5F6B7A"/>
    </Style>
    <Style x:Key="Banner" TargetType="Border">
      <Setter Property="Background" Value="#FFFFF4E5"/>
      <Setter Property="BorderBrush" Value="#FFF0B45A"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="CornerRadius" Value="4"/>
      <Setter Property="Padding" Value="12,6"/>
      <Setter Property="Margin" Value="0,8,0,0"/>
    </Style>
    <Style x:Key="CellText" TargetType="TextBlock">
      <Setter Property="Margin" Value="4,2"/>
      <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
    </Style>
  </Window.Resources>

  <Grid Margin="14">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- Signing identity -->
    <Border Grid.Row="0" Style="{StaticResource Card}">
      <DockPanel>
        <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
          <ComboBox x:Name="SourceCombo" Width="200" Margin="0,0,8,0" VerticalContentAlignment="Center"
                    AutomationProperties.Name="Signing source">
            <ComboBoxItem Content="Certificate store" Tag="Store"/>
            <ComboBoxItem Content="Artifact Signing" Tag="ArtifactSigning"/>
            <ComboBoxItem Content="Digest signing library" Tag="Dlib"/>
          </ComboBox>
          <Button x:Name="ChangeIdentityButton" Content="_Change..." AutomationProperties.Name="Change signing identity"/>
        </StackPanel>
        <StackPanel VerticalAlignment="Center" Margin="0,0,12,0">
          <TextBlock x:Name="IdentityCaption" Text="Signing certificate" Style="{StaticResource Caption}"/>
          <TextBlock x:Name="IdentityText" FontSize="14" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
          <TextBlock x:Name="IdentityDetailText" FontSize="11" Foreground="#FF5F6B7A" TextTrimming="CharacterEllipsis"/>
        </StackPanel>
      </DockPanel>
    </Border>

    <!-- Environment banners -->
    <StackPanel Grid.Row="1">
      <Border x:Name="SipBanner" Style="{StaticResource Banner}" Visibility="Collapsed">
        <DockPanel>
          <Button x:Name="SipActionButton" DockPanel.Dock="Right" VerticalAlignment="Center" Margin="12,0,0,0"/>
          <TextBlock x:Name="SipText" TextWrapping="Wrap" VerticalAlignment="Center"/>
        </DockPanel>
      </Border>
      <Border x:Name="SignToolBanner" Style="{StaticResource Banner}" Visibility="Collapsed">
        <DockPanel>
          <Button x:Name="SignToolActionButton" DockPanel.Dock="Right" VerticalAlignment="Center" Margin="12,0,0,0" Content="Set path..."/>
          <TextBlock x:Name="SignToolText" TextWrapping="Wrap" VerticalAlignment="Center"/>
        </DockPanel>
      </Border>
    </StackPanel>

    <!-- Drop zone -->
    <Grid Grid.Row="2" Margin="0,8,0,0" MinHeight="96">
      <Rectangle Stroke="#FF9AA5B1" StrokeThickness="1.5" StrokeDashArray="5 3" RadiusX="6" RadiusY="6" Fill="White"/>
      <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="16,10">
        <TextBlock Text="Drop files or folders anywhere in this window" FontSize="16" FontWeight="SemiBold" HorizontalAlignment="Center"/>
        <TextBlock x:Name="DropHint" Foreground="#FF5F6B7A" Margin="0,4,0,8" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
          <Button x:Name="AddFilesButton" Content="Add _files..." Margin="0,0,8,0" ToolTip="Ctrl+O"/>
          <Button x:Name="AddFolderButton" Content="Add f_older..." Margin="0,0,8,0" ToolTip="Ctrl+Shift+O"/>
          <Button x:Name="RemoveButton" Content="_Remove" Margin="0,0,8,0" ToolTip="Delete"/>
          <Button x:Name="RescanButton" Content="Re_scan" Margin="0,0,8,0" ToolTip="F5"/>
          <Button x:Name="ClearButton" Content="Clear list"/>
        </StackPanel>
      </StackPanel>
    </Grid>

    <!-- Filters -->
    <DockPanel Grid.Row="3" Margin="0,8,0,0" LastChildFill="False">
      <TextBlock Text="Format" VerticalAlignment="Center" Margin="0,0,6,0"/>
      <ComboBox x:Name="FormatFilter" Width="170" Margin="0,0,14,0" AutomationProperties.Name="Format filter"/>
      <TextBlock Text="Status" VerticalAlignment="Center" Margin="0,0,6,0"/>
      <ComboBox x:Name="StatusFilter" Width="120" Margin="0,0,14,0" AutomationProperties.Name="Status filter"/>
      <TextBlock Text="Search" VerticalAlignment="Center" Margin="0,0,6,0"/>
      <TextBox x:Name="SearchBox" Width="220" VerticalContentAlignment="Center" Padding="4,2" AutomationProperties.Name="Search file names" ToolTip="Ctrl+F"/>
      <TextBlock x:Name="FilterCountText" DockPanel.Dock="Right" VerticalAlignment="Center" Foreground="#FF5F6B7A"/>
    </DockPanel>

    <!-- Options -->
    <Expander x:Name="OptionsExpander" Grid.Row="4" Margin="0,8,0,0" Header="Signing options" IsExpanded="False">
      <Border Style="{StaticResource Card}" Margin="0,6,0,0">
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="24"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
          </Grid.ColumnDefinitions>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
          </Grid.RowDefinitions>

          <TextBlock Grid.Row="0" Grid.Column="0" Text="Engine" VerticalAlignment="Center" Margin="0,3,8,3"/>
          <ComboBox x:Name="EngineCombo" Grid.Row="0" Grid.Column="1" Margin="0,3" AutomationProperties.Name="Signing engine">
            <ComboBoxItem Content="Automatic" Tag="Auto"/>
            <ComboBoxItem Content="SignTool" Tag="SignTool"/>
            <ComboBoxItem Content="PowerShell" Tag="PowerShell"/>
          </ComboBox>

          <TextBlock Grid.Row="1" Grid.Column="0" Text="File digest" VerticalAlignment="Center" Margin="0,3,8,3"/>
          <ComboBox x:Name="DigestCombo" Grid.Row="1" Grid.Column="1" Margin="0,3" AutomationProperties.Name="File digest algorithm">
            <ComboBoxItem Content="SHA256" Tag="SHA256"/>
            <ComboBoxItem Content="SHA384" Tag="SHA384"/>
            <ComboBoxItem Content="SHA512" Tag="SHA512"/>
          </ComboBox>

          <TextBlock Grid.Row="2" Grid.Column="0" Text="Timestamp" VerticalAlignment="Center" Margin="0,3,8,3"/>
          <ComboBox x:Name="TimestampModeCombo" Grid.Row="2" Grid.Column="1" Margin="0,3" AutomationProperties.Name="Timestamp type">
            <ComboBoxItem Content="RFC 3161" Tag="Rfc3161"/>
            <ComboBoxItem Content="Authenticode (legacy)" Tag="Authenticode"/>
            <ComboBoxItem Content="None" Tag="None"/>
          </ComboBox>

          <TextBlock Grid.Row="3" Grid.Column="0" Text="Timestamp server" VerticalAlignment="Center" Margin="0,3,8,3"/>
          <TextBox x:Name="TimestampBox" Grid.Row="3" Grid.Column="1" Margin="0,3" Padding="4,2" VerticalContentAlignment="Center" AutomationProperties.Name="Timestamp server"/>

          <TextBlock Grid.Row="4" Grid.Column="0" Text="Engine in use" VerticalAlignment="Center" Margin="0,3,8,3" Style="{StaticResource Caption}"/>
          <TextBlock x:Name="EngineInfoText" Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="4" VerticalAlignment="Center" Margin="0,3" Style="{StaticResource Caption}" TextWrapping="Wrap"/>

          <CheckBox x:Name="DualSignCheck" Grid.Row="0" Grid.Column="3" Grid.ColumnSpan="2" Margin="0,5" Content="Dual sign executables and cabinets (SHA1 + selected digest)"/>
          <CheckBox x:Name="SkipValidCheck" Grid.Row="1" Grid.Column="3" Grid.ColumnSpan="2" Margin="0,5" Content="Skip files that already have a valid signature" IsChecked="True"/>
          <CheckBox x:Name="ClearVbaCheck" Grid.Row="2" Grid.Column="3" Grid.ColumnSpan="2" Margin="0,5" Content="Remove existing VBA signatures before signing Office files"/>
          <TextBlock Grid.Row="3" Grid.Column="3" Text="Description" VerticalAlignment="Center" Margin="0,3,8,3"/>
          <DockPanel Grid.Row="3" Grid.Column="4">
            <TextBox x:Name="DescriptionUrlBox" DockPanel.Dock="Right" Width="220" Margin="8,3,0,3" Padding="4,2" AutomationProperties.Name="Description URL" ToolTip="Description URL (signtool /du)"/>
            <TextBox x:Name="DescriptionBox" Margin="0,3" Padding="4,2" AutomationProperties.Name="Description" ToolTip="Description (signtool /d)"/>
          </DockPanel>
        </Grid>
      </Border>
    </Expander>

    <!-- File list -->
    <DataGrid x:Name="FileGrid" Grid.Row="5" Margin="0,8,0,0" AutoGenerateColumns="False" IsReadOnly="True"
              CanUserAddRows="False" HeadersVisibility="Column" GridLinesVisibility="Horizontal"
              HorizontalGridLinesBrush="#FFE6E9EE" SelectionMode="Extended" Background="White" BorderBrush="#FFD5D9E0"
              EnableRowVirtualization="True" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
              AutomationProperties.Name="Files">
      <DataGrid.Columns>
        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="72" SortMemberPath="Status">
          <DataGridTextColumn.ElementStyle>
            <Style TargetType="TextBlock" BasedOn="{StaticResource CellText}">
              <Setter Property="FontWeight" Value="SemiBold"/>
              <Style.Triggers>
                <DataTrigger Binding="{Binding Status}" Value="Signed"><Setter Property="Foreground" Value="#FF1E7B34"/></DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Verified"><Setter Property="Foreground" Value="#FF1E7B34"/></DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Failed"><Setter Property="Foreground" Value="#FFC42B1C"/></DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Skipped"><Setter Property="Foreground" Value="#FF7A8594"/></DataTrigger>
              </Style.Triggers>
            </Style>
          </DataGridTextColumn.ElementStyle>
        </DataGridTextColumn>
        <DataGridTextColumn Header="File" Binding="{Binding Name}" Width="200">
          <DataGridTextColumn.ElementStyle>
            <Style TargetType="TextBlock" BasedOn="{StaticResource CellText}">
              <Setter Property="ToolTip" Value="{Binding Path}"/>
            </Style>
          </DataGridTextColumn.ElementStyle>
        </DataGridTextColumn>
        <DataGridTextColumn Header="Format" Binding="{Binding Format}" Width="130" ElementStyle="{StaticResource CellText}"/>
        <DataGridTextColumn Header="Signature" Binding="{Binding SignatureText}" Width="170">
          <DataGridTextColumn.ElementStyle>
            <Style TargetType="TextBlock" BasedOn="{StaticResource CellText}">
              <Setter Property="ToolTip" Value="{Binding SignatureText}"/>
            </Style>
          </DataGridTextColumn.ElementStyle>
        </DataGridTextColumn>
        <DataGridTextColumn Header="Details" Binding="{Binding Detail}" Width="*">
          <DataGridTextColumn.ElementStyle>
            <Style TargetType="TextBlock" BasedOn="{StaticResource CellText}">
              <Setter Property="ToolTip" Value="{Binding Detail}"/>
            </Style>
          </DataGridTextColumn.ElementStyle>
        </DataGridTextColumn>
        <DataGridTextColumn Header="Folder" Binding="{Binding Folder}" Width="220">
          <DataGridTextColumn.ElementStyle>
            <Style TargetType="TextBlock" BasedOn="{StaticResource CellText}">
              <Setter Property="ToolTip" Value="{Binding Folder}"/>
            </Style>
          </DataGridTextColumn.ElementStyle>
        </DataGridTextColumn>
      </DataGrid.Columns>
      <DataGrid.ContextMenu>
        <ContextMenu>
          <MenuItem x:Name="CopyPathMenu" Header="Copy path"/>
          <MenuItem x:Name="OpenFolderMenu" Header="Open containing folder"/>
          <Separator/>
          <MenuItem x:Name="RemoveMenu" Header="Remove from list" InputGestureText="Delete"/>
        </ContextMenu>
      </DataGrid.ContextMenu>
    </DataGrid>

    <!-- Summary and actions -->
    <DockPanel Grid.Row="6" Margin="0,10,0,0">
      <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
        <Button x:Name="ExportButton" Content="_Export CSV..." Margin="0,0,8,0" ToolTip="Ctrl+E"/>
        <Button x:Name="VerifyButton" Content="_Verify" Margin="0,0,8,0" ToolTip="Check the signatures of the listed files"/>
        <Button x:Name="CancelButton" Content="Cancel" Margin="0,0,8,0" IsEnabled="False"/>
        <Button x:Name="SignButton" Content="Sign 0 files" FontWeight="SemiBold" Padding="20,6" IsEnabled="False" ToolTip="Ctrl+Enter"/>
      </StackPanel>
      <TextBlock x:Name="SummaryText" VerticalAlignment="Center" Foreground="#FF5F6B7A" TextTrimming="CharacterEllipsis"/>
    </DockPanel>
    <DockPanel Grid.Row="7" Margin="0,8,0,0">
      <TextBlock x:Name="VersionText" DockPanel.Dock="Right" Style="{StaticResource Caption}" VerticalAlignment="Center" Margin="12,0,0,0"/>
      <ProgressBar x:Name="Progress" Height="6" Visibility="Hidden"/>
    </DockPanel>
  </Grid>
</Window>
